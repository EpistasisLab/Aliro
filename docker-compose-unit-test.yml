version: '3'

services: 
  unit_tester:
    build: ./tests/unit
    working_dir: /appsrc
    command: "sh tests/unit_test_runner.sh"
    #command: ls
    tty: true
    stdin_open: true
    volumes: 
      - "./:/appsrc"